[phases.setup]
# FIX #2: tambah dejavu_fonts + fontconfig agar FFmpeg drawtext tidak gagal di container
nixPkgs = ["python313", "ffmpeg", "fontconfig", "dejavu_fonts"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn main:app --host 0.0.0.0 --port $PORT"
